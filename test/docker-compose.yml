services:
  qgis:
    # container_name: qgis_testing
    # image: qgis/qgis:latest
    # volumes:
    #   - /tmp/.X11-unix:/tmp/.X11-unix
    #   - ../arches_project:/tests_directory
    # environment:
    #   - DISPLAY=unix$DISPLAY
    # command: qgis 

    container_name: qgis_testing
    build: 
      context: ..
      dockerfile: ./test/Dockerfile
    # volumes:
    #   - ../arches_project/tests/:/tests_directory
    entrypoint: ["tail", "-f", "/dev/null"]  
