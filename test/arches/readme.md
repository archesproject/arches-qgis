# Arches docker containers

The idea here is to provide a barebones, headless, containerised Arches instance for testing with the QGIS plugin.

The Arches project will be loaded with an Oauth2 application ready to be used with the plugin tests.
It will have pre-built resource models for testing.